# 钢琴练习助手 - 如何运行

> 启动脚本会自动检测 Python（py 或 python）和依赖，缺失时再安装，无需每次联网。

## 依赖安装（首次或更新后执行一次）

- **后端**：在项目根目录执行  
  `py -m pip install -r backend\requirements.txt`  
  （若没有 `py`，可改用 `python -m pip install -r backend\requirements.txt`）
- **前端**：在项目根目录执行  
  `cd frontend` → `npm install`

## 方式一：一键脚本（推荐）

在项目根目录双击 **`run.bat`**，或在命令行执行：

```bash
run.bat
```

脚本会自动安装/检查依赖，先启动后端（新窗口），再启动前端。  
- 后端地址：http://127.0.0.1:8000  
- 前端地址：http://localhost:5173  

关闭「PianoTutor-Backend」窗口可停止后端；关闭运行前端的命令行窗口可停止前端。

## 方式二：npm 同时启前后端（单窗口）

在项目根目录执行：

```bash
npm install
npm run start
```

会同时启动后端和前端（同一终端）。需已安装 Node.js，且后端依赖已用上面的 pip 命令装好。

## 方式三：分别手动启动（Web）

1. **后端**（在项目根目录执行）：  
   `py -m uvicorn backend.app:app --port 8000`  
   若在 `backend` 目录下，则用：`py -m uvicorn app:app --port 8000`
2. **前端**：新开一个终端，在项目根目录执行：  
   `cd frontend` → `npm run dev`

然后浏览器访问 http://localhost:5173 使用应用。

---

## 方式四：Beta 桌面版（Electron，推荐给内测用户）

一键在窗口里打开当前 Web 版（本机启动后端 + 内置浏览器），**无需再开浏览器标签**。详细说明、打包步骤与体验用户环境要求见 **`docs/BETA.md`**。

**开发者本机试跑：**

```bash
npm run beta:desktop:fresh
```

（会先构建 `frontend/dist`，再启动 Electron。若已构建过前端，可用：`npm run beta:desktop`。）

**打 Windows 便携版（默认，推荐内测）：**

```bash
npm run beta:pack
```

产物在 **`desktop/dist-out/`**，默认是 **`Piano Tutor Beta x.x.x.zip`**：解压后双击其中的 **`Piano Tutor Beta.exe`** 即可（不依赖 NSIS，适合国内打包）。  
单文件 exe / 安装包见 **`docs/BETA.md`**。

> **发给用户的 zip 包**：`npm run beta:pack` 会先用 **PyInstaller** 打出 `piano-backend.exe` 再打包，用户侧**一般无需安装 Python**。仅在**你自己机器上打包**时需要 Python + 依赖。MP3 比对仍可能需要本机 **ffmpeg**（见 `docs/BETA.md`）。  
> **更新提醒**：在 `desktop/update-config.json` 配置 `manifestUrl` 指向网上的 `release.json` 后，用户启动应用可收到新版本弹窗，并从浏览器下载新 zip（详见 `docs/BETA.md`「版本更新提醒」）。  
> **助手免用户填 Key**：将 `desktop/publisher.env.example` 复制为 `desktop/publisher.env` 并填写百炼 Key 后再 `npm run beta:pack`，详见 `docs/BETA.md`。

---

## 方式五：GUI 运行（旧版比对窗口）

不启动浏览器，直接打开本地图形界面进行乐谱上传与比对。

### 最省事：桌面快捷方式（无黑框）

1. **创建快捷方式**（只需做一次）：  
   在项目根目录**双击** **`创建桌面快捷方式.bat`**，按提示完成即可。
2. 之后在桌面双击 **「Piano Tutor GUI」** 即可启动，不会弹出黑色命令行窗口。

### 其他启动方式

- **无黑框**：双击 **`run_gui_launcher.vbs`**（静默装依赖后直接开 GUI）
- **带命令行窗口**：双击 **`run_gui.bat`**（会先检查依赖，窗口可关）
- **命令行**：在项目根目录执行：
  ```bash
  py run_gui.py
  ```
  或先进入 backend 目录：
  ```bash
  cd backend
  py audio_compare_gui.py
  ```
  > 若 `py` 不可用，改用 `python`；需先安装依赖：`py -m pip install -r backend\requirements.txt`

会弹出 **Piano Tutor - Pitch Compare** 窗口，可：
   - 选择标准乐谱（MusicXML/MIDI 或音频）
   - 选择用户弹奏音频
   - 可选填小节范围后点击「开始比对」

GUI 与 Web 共用同一套后端逻辑（解析、转录、比对），二者择一使用即可。

---

## 练习记录与智能体基础设施（与核心比对独立）

核心比对接口**只**返回 `accuracy` 与 `errors`；练习存档走单独 API，避免智能体相关功能影响主流程。

- 勾选「保存练习记录」时：前端会先请求  
  `compare?include_note_snapshots=true`（响应中附带 `reference_notes` / `played_notes`，体积较大），  
  再 **POST** `/api/practice/sessions` 写入 `data/practice/practice.db`（SQLite）。

- 指标含：音准、错误类型占比、节奏均值/中位数/**标准差、CV、P90**、**弹奏稳定性（`stability`）**、**薄弱小节排序（`weak_measures_ranked`）** 等（`schema_version: 3`）。`POST /api/score/{id}/compare` 响应中也会带 **`metrics`**（无需保存即可看本次稳定度与薄弱小节）。字段说明见 **`docs/METRICS.md`**。

- **MusicXML**：后端对同一文件按磁盘修改时间做**内存缓存**，重复比对同一谱时解析更快（多进程各有一份缓存）。

- 关闭练习 API（排查问题时）：设置环境变量 **`ENABLE_PRACTICE_API=false`**，所有 `/api/practice/*` 返回 503。

- 模块与数据流见 **`docs/ARCHITECTURE.md`**。

- **Web 前端**：页面底部 **「4. 近期练习概况」** 会请求 `GET /api/practice/summary`；保存练习记录成功后会自动刷新；也可手动点「刷新概况」。

- **Web 第 3 步**：弹奏文件可选 **wav/mp3 等录音**（走转录，可勾多声部）或 **MIDI**（直接解析音符，多声部选项无效）。详细自测步骤见 **`docs/TEST_CHECKLIST.md`**。

### 智能练习建议（阿里云百炼 · 阶段 1）

1. 在阿里云开通百炼并创建 **API-Key**。  
2. 在项目根目录复制 **`.env.example`** 为 **`.env`**，填入：  
   `DASHSCOPE_API_KEY=你的Key`  
   可选：`DASHSCOPE_MODEL=qwen-turbo`（或 `qwen-plus` / `qwen-max`）。  
3. 安装依赖（若尚未安装）：`py -m pip install -r backend\requirements.txt`（需 **httpx**、**python-dotenv**）。  
4. **从项目根**启动后端（与现有一致）：`py -m uvicorn backend.app:app --port 8000`，以便加载根目录 `.env`。  
5. Web 页 **「5. 智能练习建议」** 点「生成练习建议」；筛选与 **第 4 节** 相同（是否仅当前乐谱、`最近条数`）。

未配置 Key 时接口返回 **503**；调用失败会显示百炼返回的错误信息。

### API 摘要

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/score/{id}/compare` | 核心比对；弹奏文件为 **录音** 或 **MIDI**（.mid/.midi）；可选 `include_note_snapshots=true` |
| POST | `/api/practice/sessions` | JSON body：`user_id`, `score_id`, `accuracy`, `errors`，可选 `reference_notes`, `played_notes` |
| GET | `/api/practice/sessions` | 列表 |
| GET | `/api/practice/sessions/{id}` | 详情 |
| GET | `/api/practice/summary` | 最近 N 次会话聚合（`user_id`, 可选 `score_id`, `last_n`） |
| POST | `/api/practice/events` | 记录事件：`user_id`, `event_type`, 可选 `score_id`, `session_id`, `payload` |
| GET | `/api/practice/events` | 事件列表 |
| POST | `/api/assistant/suggest` | 智能练习建议 JSON：`user_id`, 可选 `score_id`, `last_n`（需 `DASHSCOPE_API_KEY`） |
