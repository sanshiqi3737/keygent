# 发布者专用：复制本文件为同目录下的 publisher.env（勿提交 publisher.env 到 Git）
# 填写后执行 npm run beta:pack，内测用户无需再配置助手密钥（密钥会打进安装包，注意泄露与费用风险）

DASHSCOPE_API_KEY=sk-your-key-here
DASHSCOPE_MODEL=qwen-turbo

# 可选：管理后台口令（若项目启用）
# ADMIN_PASSCODE=your-admin-code
